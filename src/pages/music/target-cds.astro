---
import MainLayout from "../../layouts/MainLayout.astro";
import { getCollection } from "astro:content";
import type { CollectionEntry } from "astro:content";
import BackButton from "../../components/BackButton.astro";
import BackToTop from "../../components/BackToTop.astro";
import TargetCdCard from "../../components/TargetCdCard.astro";

const aCds = await getCollection("targetCds", (entry) => {
  return entry.data.artist.toLowerCase().startsWith("a");
});
const bCds = await getCollection("targetCds", (entry) => {
  return entry.data.artist.toLowerCase().startsWith("b");
});
const cCds = await getCollection("targetCds", (entry) => {
  return entry.data.artist.toLowerCase().startsWith("c");
});
const dCds = await getCollection("targetCds", (entry) => {
  return entry.data.artist.toLowerCase().startsWith("d");
});
const eCds = await getCollection("targetCds", (entry) => {
  return entry.data.artist.toLowerCase().startsWith("e");
});
const fCds = await getCollection("targetCds", (entry) => {
  return entry.data.artist.toLowerCase().startsWith("f");
});
const gCds = await getCollection("targetCds", (entry) => {
  return entry.data.artist.toLowerCase().startsWith("g");
});
const hCds = await getCollection("targetCds", (entry) => {
  return entry.data.artist.toLowerCase().startsWith("h");
});
const iCds = await getCollection("targetCds", (entry) => {
  return entry.data.artist.toLowerCase().startsWith("i");
});
const jCds = await getCollection("targetCds", (entry) => {
  return entry.data.artist.toLowerCase().startsWith("j");
});
const kCds = await getCollection("targetCds", (entry) => {
  return entry.data.artist.toLowerCase().startsWith("k");
});
const lCds = await getCollection("targetCds", (entry) => {
  return entry.data.artist.toLowerCase().startsWith("l");
});
const mCds = await getCollection("targetCds", (entry) => {
  return entry.data.artist.toLowerCase().startsWith("m");
});
const nCds = await getCollection("targetCds", (entry) => {
  return entry.data.artist.toLowerCase().startsWith("n");
});
const oCds = await getCollection("targetCds", (entry) => {
  return entry.data.artist.toLowerCase().startsWith("o");
});
const pCds = await getCollection("targetCds", (entry) => {
  return entry.data.artist.toLowerCase().startsWith("p");
});
const qCds = await getCollection("targetCds", (entry) => {
  return entry.data.artist.toLowerCase().startsWith("q");
});
const rCds = await getCollection("targetCds", (entry) => {
  return entry.data.artist.toLowerCase().startsWith("r");
});
const sCds = await getCollection("targetCds", (entry) => {
  return entry.data.artist.toLowerCase().startsWith("s");
});
const tCds = await getCollection("targetCds", (entry) => {
  return entry.data.artist.toLowerCase().startsWith("t");
});
const uCds = await getCollection("targetCds", (entry) => {
  return entry.data.artist.toLowerCase().startsWith("u");
});
const vCds = await getCollection("targetCds", (entry) => {
  return entry.data.artist.toLowerCase().startsWith("v");
});
const wCds = await getCollection("targetCds", (entry) => {
  return entry.data.artist.toLowerCase().startsWith("w");
});
const xCds = await getCollection("targetCds", (entry) => {
  return entry.data.artist.toLowerCase().startsWith("x");
});
const yCds = await getCollection("targetCds", (entry) => {
  return entry.data.artist.toLowerCase().startsWith("y");
});
const zCds = await getCollection("targetCds", (entry) => {
  return entry.data.artist.toLowerCase().startsWith("z");
});
---

<MainLayout>
  <BackButton />
  <h1>Target CD Collection</h1>

  <section
    id='jump-to-a'
    class='jump-to-link'
  >
    <h2>A</h2>
    <div class='target-card-wrapper'>
      {aCds && aCds.map((cd) => <TargetCdCard cd={cd} />)}
    </div>
  </section>
  <section
    id='jump-to-b'
    class='jump-to-link'
  >
    <h2>B</h2>
    <div class='target-card-wrapper'>
      {bCds && bCds.map((cd) => <TargetCdCard cd={cd} />)}
    </div>
  </section>
  <section
    id='jump-to-c'
    class='jump-to-link'
  >
    <h2>C</h2>
    <div class='target-card-wrapper'>
      {cCds && cCds.map((cd) => <TargetCdCard cd={cd} />)}
    </div>
  </section>
  <section
    id='jump-to-d'
    class='jump-to-link'
  >
    <h2>D</h2>
    <div class='target-card-wrapper'>
      {dCds && dCds.map((cd) => <TargetCdCard cd={cd} />)}
    </div>
  </section>
  <section
    id='jump-to-e'
    class='jump-to-link'
  >
    <h2>E</h2>
    <div class='target-card-wrapper'>
      {eCds && eCds.map((cd) => <TargetCdCard cd={cd} />)}
    </div>
  </section>
  <section
    id='jump-to-f'
    class='jump-to-link'
  >
    <h2>F</h2>
    <div class='target-card-wrapper'>
      {fCds && fCds.map((cd) => <TargetCdCard cd={cd} />)}
    </div>
  </section>
  <section
    id='jump-to-a'
    class='jump-to-link'
  >
    <h2>G</h2>
    <div class='target-card-wrapper'>
      {gCds && gCds.map((cd) => <TargetCdCard cd={cd} />)}
    </div>
  </section>
  <section
    id='jump-to-g'
    class='jump-to-link'
  >
    <h2>H</h2>
    <div class='target-card-wrapper'>
      {hCds && hCds.map((cd) => <TargetCdCard cd={cd} />)}
    </div>
  </section>
  <section
    id='jump-to-i'
    class='jump-to-link'
  >
    <h2>I</h2>
    <div class='target-card-wrapper'>
      {iCds && iCds.map((cd) => <TargetCdCard cd={cd} />)}
    </div>
  </section>
  <section
    id='jump-to-j'
    class='jump-to-link'
  >
    <h2>J</h2>
    <div class='target-card-wrapper'>
      {jCds && jCds.map((cd) => <TargetCdCard cd={cd} />)}
    </div>
  </section>
  <section
    id='jump-to-k'
    class='jump-to-link'
  >
    <h2>K</h2>
    <div class='target-card-wrapper'>
      {kCds && kCds.map((cd) => <TargetCdCard cd={cd} />)}
    </div>
  </section>
  <section
    id='jump-to-l'
    class='jump-to-link'
  >
    <h2>L</h2>
    <div class='target-card-wrapper'>
      {lCds && lCds.map((cd) => <TargetCdCard cd={cd} />)}
    </div>
  </section>
  <section
    id='jump-to-m'
    class='jump-to-link'
  >
    <h2>M</h2>
    <div class='target-card-wrapper'>
      {mCds && mCds.map((cd) => <TargetCdCard cd={cd} />)}
    </div>
  </section>
  <section
    id='jump-to-n'
    class='jump-to-link'
  >
    <h2>N</h2>
    <div class='target-card-wrapper'>
      {nCds && nCds.map((cd) => <TargetCdCard cd={cd} />)}
    </div>
  </section>
  <section
    id='jump-to-o'
    class='jump-to-link'
  >
    <h2>O</h2>
    <div class='target-card-wrapper'>
      {oCds && oCds.map((cd) => <TargetCdCard cd={cd} />)}
    </div>
  </section>
  <section
    id='jump-to-p'
    class='jump-to-link'
  >
    <h2>P</h2>
    <div class='target-card-wrapper'>
      {pCds && pCds.map((cd) => <TargetCdCard cd={cd} />)}
    </div>
  </section>
  <section
    id='jump-to-q'
    class='jump-to-link'
  >
    <h2>Q</h2>
    <div class='target-card-wrapper'>
      {qCds && qCds.map((cd) => <TargetCdCard cd={cd} />)}
    </div>
  </section>
  <section
    id='jump-to-r'
    class='jump-to-link'
  >
    <h2>R</h2>
    <div class='target-card-wrapper'>
      {rCds && rCds.map((cd) => <TargetCdCard cd={cd} />)}
    </div>
  </section>
  <section
    id='jump-to-s'
    class='jump-to-link'
  >
    <h2>S</h2>
    <div class='target-card-wrapper'>
      {sCds && sCds.map((cd) => <TargetCdCard cd={cd} />)}
    </div>
  </section>
  <section
    id='jump-to-t'
    class='jump-to-link'
  >
    <h2>T</h2>
    <div class='target-card-wrapper'>
      {tCds && tCds.map((cd) => <TargetCdCard cd={cd} />)}
    </div>
  </section>
  <section
    id='jump-to-u'
    class='jump-to-link'
  >
    <h2>U</h2>
    <div class='target-card-wrapper'>
      {uCds && uCds.map((cd) => <TargetCdCard cd={cd} />)}
    </div>
  </section>
  <section
    id='jump-to-v'
    class='jump-to-link'
  >
    <h2>V</h2>
    <div class='target-card-wrapper'>
      {vCds && vCds.map((cd) => <TargetCdCard cd={cd} />)}
    </div>
  </section>
  <section
    id='jump-to-w'
    class='jump-to-link'
  >
    <h2>W</h2>
    <div class='target-card-wrapper'>
      {wCds && wCds.map((cd) => <TargetCdCard cd={cd} />)}
    </div>
  </section>
  <section
    id='jump-to-x'
    class='jump-to-link'
  >
    <h2>X</h2>
    <div class='target-card-wrapper'>
      {xCds && xCds.map((cd) => <TargetCdCard cd={cd} />)}
    </div>
  </section>
  <section
    id='jump-to-y'
    class='jump-to-link'
  >
    <h2>Y</h2>
    <div class='target-card-wrapper'>
      {yCds && yCds.map((cd) => <TargetCdCard cd={cd} />)}
    </div>
  </section>
  <section
    id='jump-to-z'
    class='jump-to-link'
  >
    <h2>Z</h2>
    <div class='target-card-wrapper'>
      {zCds && zCds.map((cd) => <TargetCdCard cd={cd} />)}
    </div>
  </section>
  <BackToTop />
</MainLayout>

<style>
  .jump-to-link {
    padding: 1rem 0rem;
    /* border-top: 1px solid var(--text-color-700); */
    h2 {
      border-bottom: 1px solid var(--text-color-700);
      padding-block-end: 1rem;
      margin-block-end: 1rem;
    }
  }
  .target-card-wrapper {
    /* i tried dividing by 2, but it wrapped too early, so dividing by 3 and 
    not counting the gap makes two columns that wrap at a nice point */
    --max-card-width: calc(var(--max-width) / 3);

    display: grid;
    grid-template-columns: repeat(
      auto-fit,
      minmax(min(100%, var(--max-card-width)), 1fr)
    );
    align-items: center;
    gap: 1rem;
  }
</style>
